/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // 이 부분을 반드시 추가해야 합니다!
  
  // 깃허브 저장소 이름이 'phone-calculator'라면 아래 주석을 해제하고 이름을 맞춰주세요.
  // CSS나 이미지가 깨지는 것을 방지합니다.
  // basePath: '/phone-calculator',
};

export default nextConfig;